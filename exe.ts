// Esta função recebe dois números como argumentos e retorna seu produto
function multiply(a: number, b: number): number {
    return a * b;
}

  // Esta função recebe uma string como argumento e retorna a string "Olá" concatenada com a string de entrada
function greet(name: string): string {
    return `Olá ${name}`;
}